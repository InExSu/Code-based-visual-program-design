## Автоматический запуск dharma_2_Mermaid.js при изменении dharma.js настроен

### 🎯 Созданы множественные решения для автоматизации:

## 1️⃣ NPM Скрипты (`package.json`)

Добавлены новые команды для удобного запуска:

```bash
npm run mermaid          # Генерация диаграммы Mermaid
npm run watch:karma      # Генерация функций karma
npm run build            # Выполнить обе операции
```

## 2️⃣ VSCode Задачи (`.vscode/tasks.json`)

Созданы настраиваемые задачи:
- **Generate Mermaid Diagram** - выполнить `dharma_2_Mermaid.js`
- **Generate Karma Functions** - выполнить `aKarma_Make.js`
- **Build All** - выполнить обе задачи последовательно

**Запуск:** `Ctrl+Shift+P` → "Tasks: Run Task" → выбрать нужную задачу

## 3️⃣ Автосохранение с задачами (`.vscode/settings.json`)

Добавлена настройка `"tasks.runOn": "save"` для автоматического выполнения задач при сохранении файлов.

## 4️⃣ File Watcher (`src/watch_dharma.js`)

Автономный скрипт, который отслеживает изменения `dharma.js` каждую секунду и автоматически запускает `dharma_2_Mermaid.js`.

**Запуск:**
```bash
node src/watch_dharma.js
```

## 🚀 Способы использования:

### Вариант 1: VSCode Автоматика (Рекомендуемый)
1. Откройте VSCode
2. При сохранении `dharma.js` автоматически выполнится задача
3. Результат будет показан в панели терминала

### Вариант 2: NPM Команды
```bash
npm run mermaid          # Разово сгенерировать диаграмму
npm run build            # Сгенерировать диаграмму и функции
```

### Вариант 3: Запуск watcher
```bash
node src/watch_dharma.js
```
Скрипт будет работать в фоне и автоматически запускать генерацию при каждом изменении `dharma.js`

### Вариант 4: VSCode Tasks вручную
`Ctrl+Shift+P` → "Tasks: Run Task" → "Generate Mermaid Diagram"

## ✅ Преимущества решений:

- **Независимость от зависимостей** - все решения используют только стандартные Node.js возможности
- **Гибкость** - можно выбрать любой способ автоматизации
- **Надежность** - несколько альтернативных вариантов
- **Удобство** - интеграция с VSCode и npm

Любое из решений автоматически запустит генерацию диаграммы Mermaid при изменении файла `dharma.js`!

